---
name: implement
description: >-
  Wear the implementer hat: one ticket, DoD, open a PR, status review.
  Slash-only (/implement).
disable-model-invocation: true
---

# /implement

Implementer only. Read `.cursor/skills/ops-loop/SKILL.md` (Implementer) and `docs/ops/workflow/DOD.md`.

1. Plan + `status: implement` already on `main`.
2. Branch from latest `main`. Boot `./scripts/app start` if verify needs the stack.
3. One ticket. Meet DoD + plan Verify.
4. Open a PR. Set ticket `pr:` + `status: review`. `board_sync.py`. Run note.
5. Do **not** merge from chat.
