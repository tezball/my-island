---
id: WF-020
aliases: [WF-020]
title: Restore live ops/ and data/leads to repo root (CI hotfix)
status: implement
priority: P0
type: bug
owner: automation-expert
area: ops
parent: "[[tickets/WF-000]]"
plan: "[[plans/WF-020]]"
pr:
blocked_reason:
---

# WF-020 — Restore live ops/ and data/leads to repo root (CI hotfix)

## Outcome

Live company OS is at repo-root `ops/` again. Living leads store is at repo-root `data/leads/`. CI unit + catalog + compose stack are green. Do **not** re-home CI paths to `docs/ops`.

## Notes

`be8562e` (“Moved all docs to the docs folder”) moved live trees under `docs/`:

- `ops/` → `docs/ops/` (broke `pip install -r ops/tests/requirements.txt` and Dockerfile `COPY ops/tests/requirements.txt`)
- `data/leads/` → `docs/data/leads/`
- `product/` → `docs/product/` (vault unit test reads `product/STACK.md`)

Restore those three live trees with `git mv`. Leave archaeology: `docs/README.md`, `docs/leads/CAMPSITE_LEADS.md`, `docs/automation/`. Root `compose.yml` stays the STACK-E2E source of truth; `ops/compose.yml` remains the include wrapper.

Ticket id is **WF-020**: WF-016/017/018 exist on main; WF-019 is claimed by open PR #31.

Failing run: https://github.com/tezball/my-island/actions/runs/34042475663

## Verify

- [ ] `ops/tests/requirements.txt` and living tickets exist at repo-root `ops/`
- [ ] `data/leads/schema.json` exists at repo root
- [ ] `product/STACK.md` exists at repo root
- [ ] `docs/ops` is gone (no duplicate canvas)
- [ ] `python3 -m pytest ops/tests -q -m "not stack"`
- [ ] `./services/catalog/mvnw -B test`
- [ ] `./scripts/dev test` (compose stack)

## Links

- Plan: [[plans/WF-020]]
- PR:
- Run:
- CI failure: https://github.com/tezball/my-island/actions/runs/34042475663
