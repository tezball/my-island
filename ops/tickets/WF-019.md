---
id: WF-019
aliases: [WF-019]
title: Repeatable place listing create/list/get sim
status: review
priority: P0
type: workflow
owner: eng-backend
area: catalog
parent: "[[tickets/E2E-001]]"
plan: "[[plans/WF-019]]"
pr: https://github.com/tezball/my-island/pull/31
blocked_reason:
---

# WF-019 — Repeatable place listing create/list/get sim

## Outcome

A small, documented, repeatable happy-path sim of catalog **create → list → get** on the STACK-E2E spine. One command, N iterations, optional actuator prometheus check. Assumes `./scripts/dev up` (no chaos).

## Notes

Workshop step 6 (Engineering). Stub already on `main` (PR #21, strip `07bf00c`). This ticket is the **sim harness**, not catalog API work and not full [[tickets/PRD-001]].

Do **not** overwrite [[tickets/WF-015]] (strip, `done`), [[tickets/WF-016]] (mcp-grafana), [[tickets/WF-017]] (chaos drill skill), or [[tickets/WF-018]] (JSON field names).

Create payload uses the live stub contract ([[tickets/WF-018]]): `categoryId`, `countyId`, `latitude`, `longitude`. Workshop exception: no auth.

Chaos stays **out** of the default sim path and out of required CI.

## Verify

- [x] One command: `./scripts/sim-place-listing.sh` after `./scripts/dev up`
- [x] Loop: `POST /api/v1/places` → `GET /api/v1/places` contains id → `GET /api/v1/places/{id}`
- [x] `/actuator/health` checked; `/actuator/prometheus` optional
- [x] `--iterations N` (or `SIM_ITERATIONS`) for smoke/load
- [x] Default path does not enable chaos / `compose.chaos.yml`
- [x] Stub create fields: `categoryId`, `countyId`, `latitude`, `longitude`
- [x] `python3 -m pytest ops/tests -q -m "not stack"` green
- [x] No rewrite of [`product/STACK.md`](../../product/STACK.md); no UI/booking

## Links

- Plan: [[plans/WF-019]]
- PR: https://github.com/tezball/my-island/pull/31
- Run: [[runs/WF-019-implement]]
- Workshop: [[tickets/E2E-001]] · [[workshops/e2e-place-stub]] · [[workflow/STACK-E2E-place-stub]]
- Do not reuse: [[tickets/WF-015]] · [[tickets/WF-016]] · [[tickets/WF-017]] · [[tickets/WF-018]]
- Canon: [`product/STACK.md`](../../product/STACK.md)
