---
id: WF-016
aliases: [WF-016]
title: STACK-E2E MCP vs compose chaos drill skill and runbook
status: ready
priority: P1
type: workflow
owner: automation-expert
area: ops
parent: "[[tickets/E2E-001]]"
plan:
pr:
blocked_reason:
---

# WF-016 — STACK-E2E MCP vs compose chaos drill skill and runbook

## Outcome

Agents can run the place-stub stack E2E drill from a skill + runbook without inventing curl: happy-path compose, HTTP create/list/get + actuators, MCP-or-HTTP observe, opt-in chaos overlay, write gaps. Chaos stays **out** of required CI.

## Notes

E2E workshop 2026-09-06 found the drill is **wired** but not **taught**:

- `./scripts/dev up` boots catalog **without** Spring profile `chaos`. Overlay is required: `docker compose -f compose.yml -f compose.chaos.yml --profile chaos up -d catalog --wait`.
- Architecture shorthand `docker compose --profile chaos up` **without** `compose.chaos.yml` leaves `SPRING_PROFILES_ACTIVE` unset — assaults never start.
- Chaos Monkey latency + exceptions fired on place HTTP (`RuntimeException: Chaos Monkey - RuntimeException` in logs). Spring default 500 JSON has **no** chaos marker in the body. `/actuator/chaosmonkey` is **404** (exposure is `health,info,prometheus` only).
- `restController` + `service` watchers together made ~70% 500s on list/get/create; `/actuator/health` stayed 200 and kill stayed off.
- No skill/runbook for “MCP vs compose” (STACK-E2E §7). Follow [[runbooks/ADD_SKILL]].

Do not expand into full [[tickets/PRD-001]]. Do not put chaos on the default compose service or CI `catalog` / `stack` jobs.

## Verify

- [ ] Skill `.cursor/skills/` with `description` that fires on E2E-001 / STACK-E2E / chaos overlay / mcp-grafana catalog scrape
- [ ] Runbook `ops/runbooks/` + row on [[runbooks/_index]] covering: `./scripts/dev up`, overlay command, create/list/get + actuators, Grafana/PromQL fallback HTTP if MCP missing, overlay down / restore default catalog
- [ ] Skill/runbook state that required CI must not set Spring profile `chaos` or compose profile `chaos`
- [ ] Row on [[workflow/SKILLS]]
- [ ] `python3 -m pytest ops/tests -q -m "not stack"` green
- [ ] No secrets; no merge; no product UI

## Links

- Workshop: [[tickets/E2E-001]] · [[workflow/STACK-E2E-place-stub]] · [[workshops/e2e-place-stub]]
- Add skill: [[runbooks/ADD_SKILL]]
- Local: [[workflow/LOCAL]] · [[workflow/MCP]]
- Sibling: [[tickets/WF-015]]
- Run: [[runs/e2e-place-stub-mcp-chaos-2026-09-06]]
- Canon: [`product/STACK.md`](../../product/STACK.md)
