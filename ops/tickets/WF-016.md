---
id: WF-016
aliases: [WF-016]
title: Cloud Agent mcp-grafana and Postgres-RO against local compose
status: review
priority: P1
type: workflow
owner: automation-expert
area: ops
parent: "[[tickets/E2E-001]]"
plan: "[[plans/WF-016]]"
pr: https://github.com/tezball/my-island/pull/46
blocked_reason:
---

# WF-016 — Cloud Agent mcp-grafana and Postgres-RO against local compose

## Outcome

A Cloud Agent on this repo can **read** local compose Grafana + Postgres the same way a laptop Cursor session does: `mcp-grafana` PromQL/LogQL (`--disable-write`) and Postgres MCP `SELECT` only. HTTP to `127.0.0.1:3030` / `5433` already works on the VM; the gap is the **MCP toolbox**, not compose.

Does **not** close [[tickets/WF-004]] (staging/prod remote MCP) or [[tickets/WF-010]] (always-on EU staging). Those stay blocked until a shared env exists. This ticket is the workshop-shaped hole: Cloud Agent + local compose.

## Notes

E2E workshop 2026-09-06 (`ops/runs/e2e-place-stub-mcp-chaos-2026-09-06.md`):

- Catalog HTTP and Grafana/Prometheus HTTP succeeded from the Cloud VM.
- Agent dynamic MCP namespaces were GitHub, Gmail, Calendar, Drive, cursor-cloud — **no** `grafana`, **no** `postgres`.
- `.cursor/mcp.json` stdio (`uvx mcp-grafana`, `npx` Postgres → `ops` db) is not attached to Cloud Agent runs. `uvx` is on PATH.
- `ops_reader` can `SELECT` `mcp_ping` on db `ops`. `SELECT` on `catalog.place` is **permission denied**.

**Split:** this PR (#46) is **docs / Cloud Agent MCP attach** only. Catalog SELECT grants (Flyway/SQL + optional `postgres-catalog` DSN) are **Engineering** on a separate agent.

**TODO Engineering grants:** no PR yet. Cross-link here when it exists. Do not duplicate grants SQL on #46.

Cloud Agent attach: a human copies `grafana` + `postgres` stdio servers onto cursor.com (MCP dropdown / team Integrations). `.cursor/environment.json` cannot register MCP. HTTP PromQL is the equivalent until dashboard stdio exists. Policy: [[workflow/MCP]].

Id note: this was drafted as WF-015 on PR #30 before the spine strip landed on `main` as [[tickets/WF-015]] (`done`). Renumbered to the next free id.

House: Grafana OSS + `mcp-grafana`, not Datadog. No secrets in notes.

## Verify

- [x] Cloud Agent session lists `grafana` and `postgres` MCP tools (or documented HTTP/SSE equivalents) against compose `127.0.0.1:3030` and `5433` — HTTP documented; stdio attach is dashboard (repo cannot register MCP)
- [x] `mcp-grafana` PromQL `up{job="catalog"}` returns 1 while `./scripts/dev up` is healthy (same signal as Grafana datasource Prometheus) — HTTP PromQL + Grafana `/api/ds/query` on the implement VM
- [ ] Postgres MCP (or ops_reader) can `SELECT` place rows in db `catalog` (SELECT only; no INSERT/UPDATE/DELETE) — **TODO Engineering grants** (no PR yet)
- [x] `--disable-write` on Grafana; no prod SQL; no tokens in `ops/` notes
- [x] Does not enable chaos in required CI
- [x] `python3 -m pytest ops/tests -q -m "not stack"` green

## Links

- Plan: [[plans/WF-016]]
- Workshop: [[tickets/E2E-001]] · [[workflow/STACK-E2E-place-stub]]
- Related (do not reopen as done): [[tickets/WF-004]], [[tickets/WF-010]], [[tickets/WF-001]]
- Siblings: [[tickets/WF-017]] · [[tickets/WF-018]]
- Policy: [[workflow/MCP]] · [[workflow/LOCAL]]
- Run: [[runs/e2e-place-stub-mcp-chaos-2026-09-06]] · [[runs/WF-016-implement]]
- Canon: [`product/STACK.md`](../../product/STACK.md)
