---
id: WF-015
aliases: [WF-015]
title: Postgres-RO SELECT on catalog.place for MCP review
status: ready
priority: P1
type: workflow
owner: automation-expert
area: ops
parent: "[[tickets/E2E-001]]"
plan:
pr:
blocked_reason:
---

# WF-015 — Postgres-RO SELECT on catalog.place for MCP review

## Outcome

`ops_reader` can `SELECT` the catalog stub’s `place` rows (and lookup tables) without write rights. Local Postgres MCP (or its documented DSN) can run that SELECT. STACK-E2E §7 optional Postgres-RO review against places then works when MCP is present.

## Notes

Found on happy-path MCP review [[runs/e2e-place-stub-mcp-2026-09-06]] (not chaos).

Today:

- `.cursor/mcp.json` DSN is `postgresql://ops_reader:ops_reader@127.0.0.1:5433/ops` ([[workflow/MCP]]).
- Init grants `ops_reader` on database `ops` only (`ops/observability/postgres-init.sql`).
- Direct `SELECT` as `ops_reader` on `catalog.place` → `permission denied for table place`.
- Row exists for the writer role `ops` on db `catalog`.

Do not give `ops_reader` INSERT/UPDATE/DELETE. Do not put connection secrets in notes. Do not enable chaos. Cloud Agent **grafana/postgres MCP servers** remain a separate gap ([[tickets/WF-004]], [[tickets/WF-010]]).

## Verify

- [ ] `ops_reader` can `SELECT id, name, slug FROM place` on database `catalog` (or a documented equivalent)
- [ ] `ops_reader` cannot `INSERT` / `UPDATE` / `DELETE` on `place`
- [ ] Local MCP pack ([[workflow/MCP]] / `.cursor/mcp.json`) documents how to run that SELECT (catalog DSN or extra server). No secrets in `ops/`
- [ ] Re-run of STACK-E2E §7 optional Postgres-RO against a created place succeeds when MCP is loaded

## Links

- Parent: [[tickets/E2E-001]]
- Related: [[tickets/WF-001]], [[tickets/WF-004]]
- Run: [[runs/e2e-place-stub-mcp-2026-09-06]]
- Canon: [[workflow/STACK-E2E-place-stub]] §7 · [[workflow/MCP]]
