---
id: WF-001
aliases: [WF-001]
title: Local compose and MCP pack
status: done
priority: P0
type: workflow
owner: eng-infra
area: ops
plan: "[[plans/WF-000]]"
pr: https://github.com/tezball/my-island/pull/6
blocked_reason:
---

# WF-001 — Local compose and MCP pack

## Outcome

`./ops/scripts/start-local.sh` brings up Grafana, Loki, Prometheus, Alertmanager, Postgres. `.cursor/mcp.json` talks to Grafana (read-only), Postgres as `ops_reader`, GitHub (PAT from env), Docker MCP Toolkit (`docker` CLI via gateway), and Playwright. Documented in [[workflow/LOCAL]] and [[workflow/MCP]].

## Notes

Landed on `main`: root `compose.yml`, `./scripts/dev up` / `./ops/scripts/start-local.sh`, `.cursor/mcp.json` (Grafana `--disable-write`, `ops_reader`, GitHub, Docker gateway, Playwright), [[workflow/LOCAL]] and [[workflow/MCP]]. Folded into [[tickets/WF-005]] ([#6](https://github.com/tezball/my-island/pull/6)). Cloud Agent MCP attach is a later gap — [[tickets/WF-016]] stays `ready`.

No application API. This is the agent substrate.
