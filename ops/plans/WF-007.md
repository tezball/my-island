---
id: WF-007
ticket: "[[tickets/WF-007]]"
status: approved
---

# Plan: Lock CEO stack and product decisions (2026-09-05)

## Outcome

`product/STACK.md` is Architecture’s CEO-locked draft (exact). Decision log, tickets, and agent notes match Spring house, Vite+React light PWA (not Next), Postgres/PostGIS, Flyway, Grafana MCP, and the STACK gap list. No consumer app.

## Out of scope

Spring/React skeleton, Playwright CI job, staging VPS, closing GitHub PRs from the agent (humans: [[tickets/WF-008]]), merging this PR.

## Steps

1. Replace `product/STACK.md` with the Architecture draft. Fix relative links only if broken.
2. Record CEO decisions in [[company/DECISIONS]].
3. Align `product/` notes and agent roster so nothing prefers Next / Neon / Vercel / FastAPI over the house.
4. File or update tickets: docs banner, stale draft PRs, STACK rewrite, gated skeleton, MCP/staging and other STACK gaps.
5. History banners on `docs/` so agents do not implement from it.

## Verify

- [ ] `product/STACK.md` matches the Architecture draft (Spring, Vite+React not Next, PostGIS, Flyway, Grafana MCP)
- [ ] `ops/company/DECISIONS.md` dated 2026-09-05
- [ ] Agent roles/runbooks do not recommend Next/Neon/Vercel/FastAPI as defaults
- [ ] Gap tickets exist; PRD skeleton tickets stay not `implement`
- [ ] `python3 -m pytest ops/tests -q -m "not stack"`
- [ ] No consumer application code

## MCP / env

Neither. Docs and vault only.
