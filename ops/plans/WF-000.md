---
id: WF-000
ticket: "[[tickets/WF-000]]"
status: approved
---

# Plan: Agent operations loop

## Outcome

A git-tracked Obsidian vault under `ops/` is the only work tracker. Agents follow plan → implement PR → review comment. Local observability compose + MCP works on a laptop. Product MVP is not in scope.

## Out of scope

Consumer app, Jenkins, prod deploy, Google/Apple auth, 500 places.

## Steps

1. Vault: HOME, BOARD (generated), tickets, templates, workflow notes.
2. `ops/compose.yml` + start/stop scripts + Grafana provisioning.
3. `.cursor/mcp.json` grafana + postgres reader.
4. `AGENTS.md`, always-on rule, `ops-loop` skill, sessionStart hook.
5. `board_sync.py` / `next_ticket.py`.
6. Document Cursor Automations; do not merge PRs from agents.

## Verify

- [ ] `python3 ops/scripts/board_sync.py` produces a board with WF-000..004
- [ ] `./ops/scripts/start-local.sh` — Grafana `/api/health` returns
- [ ] MCP grafana and postgres listed in Cursor for this repo
- [ ] A new agent session is told to read `ops/HOME.md` and not implement MVP
