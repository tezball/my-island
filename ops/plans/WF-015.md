---
id: WF-015
ticket: "[[tickets/WF-015]]"
status: approved
---

# Plan: Halfdoor brand lock and CEO role alias

## Outcome

Ops vault matches the brand lock: **Halfdoor** is public; **Inis** is internal-only; real places keep Inis/Inish names; repo stays `my-island`. Agent roster presents **CEO** as the company front door (formerly Orchestrator) without renaming the `orchestrator` file slug.

## Out of scope

- Editing [`product/NAMING.md`](../../product/NAMING.md) (Product owns it)
- Product features, consumer app, README outside `ops/`
- Mass-rename of `orchestrator` slug / wikilinks
- Merging the PR
- Retitling [[tickets/PRD-004]] (“Ireland stays marketplace” is the marketplace epic, not a public brand claim)

## Steps

1. Rewrite [[company/BRAND]]: Halfdoor public, Inis internal-only, brand-vs-place, repo `my-island`; point at `product/NAMING.md` as Product-owned, do not fork the old Cairn shortlist as current ops truth.
2. Align [[CHARTER]], [[company/_index]], [[company/PEOPLE]], [[company/DECISIONS]] (new lock row). Leave historical “Orchestrator closed PRs” lines as history.
3. Role display: [[agents/roles/orchestrator]] title **CEO**, alias formerly Orchestrator, slug unchanged. Update [[agents/_index]], [[agents/GROK_VS_CURSOR]], live role/runbook sentences that still say Orchestrator as the current front door.
4. Vault pytest so the lock does not regress. `board_sync.py`.

## Verify

- [x] Brand + charter state Halfdoor; no “public name unset” / Cairn-as-current in ops company notes
- [x] CEO front-door display; `agents/roles/orchestrator.md` still exists
- [x] `product/NAMING.md` untouched
- [x] `python3 -m pytest ops/tests -q -m "not stack"`

## MCP / env

Neither. Vault notes only.
