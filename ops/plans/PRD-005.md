---
id: PRD-005
ticket: "[[tickets/PRD-005]]"
status: approved
---

# Plan: docs old/new split / agent fence

## Outcome

**Split + fence, not banners-only.** Historical booking-platform tree lives at `docs/old/` (same layout, including `.obsidian`). `docs/new/` indexes living canon only (`product/` + `ops/`). Root `docs/README.md` explains the split and links [`product/SIGNED.md`](../../product/SIGNED.md) and [`ops/HOME.md`](../HOME.md). **Do not implement from `docs/old/`.** Delete committed `.idea/` IDE noise.

## Out of scope

- `services/` / PR #21
- `ops/workflow/e2e-place-stub.canvas`, [[tickets/E2E-001]], `ops/workshops/`
- Brand PRs #17 / #18
- Rewriting product canon substance
- Merge

## Steps

1. `git mv` all current `docs/` contents into `docs/old/` (keep tree).
2. Add `docs/new/README.md` — pointers to `product/` and `ops/` only. No invented specs.
3. Rewrite `docs/README.md` — `old/` = past booking platform (do not implement); `new/` = living-canon pointers.
4. Retarget history file pointers in repo `README.md`, `product/`, and `ops/` from `docs/...` to `docs/old/...`.
5. Keep CI green: leads provenance path, `ops/tests/test_leads.py`, vault test for the split.
6. `git rm -r .idea/` (already listed in `.gitignore`).

## Verify

- [x] `docs/old/` holds the former `docs/` tree (including `.obsidian`)
- [x] `docs/old/MVP.md` history banner points at `product/MVP.md`
- [x] `docs/old/USER_STORIES.md` and `docs/old/ROADMAP.md` have history banners
- [x] `docs/new/README.md` points at `product/` + `ops/`
- [x] `docs/README.md` links `product/SIGNED.md` and `ops/HOME.md`
- [x] `product/SIGNED.md` kill list cites `docs/old/` and this ticket
- [x] `python3 -m pytest ops/tests -q -m "not stack"`
- [x] No application code / no e2e / no services edits
- [x] Committed `.idea/` deleted from git

## MCP / env

Neither. Fast pytest only.
