---
id: WF-011
aliases: [WF-011]
title: Required Playwright on product PRs (job starts compose)
status: blocked
priority: P1
type: workflow
owner: automation-expert
area: ci
parent: "[[ops/tickets/WF-007]]"
plan:
pr:
blocked_reason: PWA exists on PRD-003. Next is a Playwright job vs job-started compose — do not add a red job. Pick after Explore is on main.
---

# WF-011 — Required Playwright on product PRs

## Outcome

When the light Vite+React PWA exists, GitHub Actions runs Playwright against compose the job starts. Merge blocked on failure. Not a screenshot-only gate. **PWA now exists** (`web/`); this ticket is the Playwright job, not the app.

## Notes

STACK gap **Required Playwright in CI**. Policy already: [[ops/workflow/CI]] — no Maven/Playwright jobs until `PRD-*` + `implement`. Client is Vite+React, **not** Next.js.

## Verify

- [ ] Playwright job uses the same compose as `./scripts/dev`
- [ ] Required check on PRs that touch the PWA/API
- [ ] No Next.js-specific CI

## Links

- Parent: [[ops/tickets/WF-007]]
- Depends: [[ops/tickets/PRD-001]], [[ops/tickets/PRD-003]]
- Policy: [[ops/workflow/CI]]
