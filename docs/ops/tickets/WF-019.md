---
id: WF-019
aliases: [WF-019]
title: Repeatable place listing create/list/get sim
status: done
priority: P0
type: workflow
owner: eng-backend
area: catalog
parent: "[[ops/tickets/E2E-001]]"
plan: "[[ops/plans/WF-019]]"
pr: https://github.com/tezball/my-island/pull/31
blocked_reason:
---

# WF-019 — Repeatable place listing create/list/get sim

## Outcome

A small, documented, repeatable happy-path sim of catalog **create → list → get** on the STACK-E2E spine. One command, N iterations, optional actuator prometheus check. Assumes `./scripts/dev up` (no chaos).

## Notes

Merged [#31](https://github.com/tezball/my-island/pull/31) 2026-09-06. Workshop step 6 (Engineering). Stub already on `main` (PR #21, strip `07bf00c`). This ticket is the **sim harness**, not catalog API work and not full [[ops/tickets/PRD-001]]. Live vault is repo-root `ops/` (hotfix #33).

Do **not** overwrite [[ops/tickets/WF-015]] (strip, `done`), [[ops/tickets/WF-016]] (mcp-grafana), [[ops/tickets/WF-017]] (chaos drill skill), or [[ops/tickets/WF-018]] (JSON field names).

Create payload uses the live stub contract ([[ops/tickets/WF-018]]): `categoryId`, `countyId`, `latitude`, `longitude`. Workshop exception: no auth.

Chaos stays **out** of the default sim path and out of required CI.

## Verify

- [x] One command: `./scripts/sim-place-listing.sh` after `./scripts/dev up`
- [x] Loop: `POST /api/v1/places` → `GET /api/v1/places` contains id → `GET /api/v1/places/{id}`
- [x] `/actuator/health` checked; `/actuator/prometheus` optional
- [x] `--iterations N` (or `SIM_ITERATIONS`) for smoke/load
- [x] Default path does not enable chaos / `compose.chaos.yml`
- [x] Stub create fields: `categoryId`, `countyId`, `latitude`, `longitude`
- [x] `python3 -m pytest ops/tests -q -m "not stack"` green
- [x] No rewrite of [`product/STACK.md`](../../product/STACK.md); no UI/booking

## Links

- Plan: [[ops/plans/WF-019]]
- PR: https://github.com/tezball/my-island/pull/31
- Run: [[ops/runs/WF-019-implement]]
- Workshop: [[ops/tickets/E2E-001]] · [[ops/workshops/e2e-place-stub]] · [[ops/workflow/STACK-E2E-place-stub]]
- Do not reuse: [[ops/tickets/WF-015]] · [[ops/tickets/WF-016]] · [[ops/tickets/WF-017]] · [[ops/tickets/WF-018]]
- Canon: [`product/STACK.md`](../../product/STACK.md)
