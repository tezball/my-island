---
id: WF-014
aliases: [WF-014]
title: Document human one-time OIDC console setup (Google / Apple)
status: blocked
priority: P2
type: workflow
owner: eng-backend
area: identity
parent: "[[ops/tickets/WF-007]]"
plan:
pr:
blocked_reason: "Post-MVP. Password auth (PRD-010) is enough for MVP. Re-open when Terry has Google (and optionally Apple) OAuth client credentials. No OIDC stub."
cssclasses:
  - ticket
---

# WF-014 — OIDC console setup (Google / Apple)

## Outcome

Human one-time IdP console work + env names so agents can wire Spring OAuth2. **Providers:** Google first; **Apple** is the same ticket (second registration), not a separate id today.

## Notes

**Post-MVP (Terry 2026-09-12).** MVP visitors use email + password only ([[ops/tickets/PRD-010]]). Do not block directory e2e on this. No OIDC stub.

When ready: create Google OAuth client (and Apple if desired), put secrets in `.env` / secret manager, document env names + redirect URIs here or in a short runbook. Agents then add Spring `registration` ids — never paste secret **values** into `ops/` notes.

Apple Sign-In needs an Apple Developer account (Team ID, Key ID, private key). If Apple is unwanted, close that slice in the plan and keep Google-only.

## Verify

- [ ] Runbook: console steps + env names (Google; Apple optional)
- [ ] Spring config is the agent interface
- [ ] Secrets not in `ops/` notes

## Links

- Auth MVP: [[ops/tickets/PRD-010]]
- Parent: [[ops/tickets/WF-007]]
- Canon: [`product/STACK.md`](../../product/STACK.md)
