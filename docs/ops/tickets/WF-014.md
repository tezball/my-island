---
id: WF-014
aliases: [WF-014]
title: Google Sign-In (OIDC console + Spring)
status: blocked
priority: P2
type: workflow
owner: eng-backend
area: identity
parent: "[[ops/tickets/WF-007]]"
plan:
pr:
blocked_reason: "Post-MVP. Password auth (PRD-010) is MVP. Re-open when Terry has Google OAuth client id + secret. No OIDC stub. Apple is [[ops/tickets/WF-033]]."
cssclasses:
  - ticket
---

# WF-014 — Google Sign-In (OIDC console + Spring)

## Outcome

Human one-time **Google** Cloud OAuth client + env names so agents can wire Spring OAuth2 (`registration` id `google`).

## Notes

**Post-MVP (Terry 2026-09-12).** MVP visitors use email + password only ([[ops/tickets/PRD-010]]). No OIDC stub.

**Apple** is a separate ticket: [[ops/tickets/WF-033]].

Mock-prod (WF-032) reuses the fishing-journals **GIS** client: JS origins `https://fishing-journals.com` + `https://app.fishing-journals.com`, token `POST /api/auth/google` `{idToken}`. That is not Spring `/oauth2/authorization/google`. This ticket remains for console documentation / any extra redirect URIs.

When ready: create/confirm Google OAuth client, put secrets in `.env` / secret manager, document env names + redirect URIs. Agents then edit Spring config — never paste secret **values** into `ops/` notes.

## Verify

- [ ] Runbook: Google console steps + env names + redirect URIs
- [ ] Spring Google registration works with password accounts still available
- [ ] Secrets not in `ops/` notes

## Links

- Auth MVP: [[ops/tickets/PRD-010]]
- Apple: [[ops/tickets/WF-033]]
- Parent: [[ops/tickets/WF-007]]
- Canon: [`product/STACK.md`](../../product/STACK.md)
