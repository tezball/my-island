---
id: WF-003
aliases: [WF-003]
title: Enable Cursor Automations for plan / implement / review
status: plan
priority: P1
type: workflow
owner: automation-expert
area: ops
plan: "[[ops/plans/WF-003]]"
pr:
blocked_reason:
---

# WF-003 — Enable Cursor Automations

## Outcome

Three cloud automations live in the Cursor Automations UI: PR reviewer, board runner, re-review on push. Spec: [[ops/workflow/AUTOMATIONS]]. Plan (paste-ready prompts + exact clicks): [[ops/plans/WF-003]].

## Notes

Requires files on `main` before prompts can `@` them. Human must finish the editor. No auto-merge. No prod. Board runner skips epics and skips product code unless the ticket is `PRD-*` **and** `status: implement`.

After enablement, paste the three dashboard URLs here (UUIDs only — no webhook keys).
