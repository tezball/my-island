---
id: WF-018
aliases: [WF-018]
title: Align E2E-001 place JSON fields with catalog stub
status: done
priority: P1
type: workflow
owner: automation-expert
area: catalog
parent: "[[ops/tickets/E2E-001]]"
plan:
pr: https://github.com/tezball/my-island/pull/37
blocked_reason:
---

# WF-018 — Align E2E-001 place JSON fields with catalog stub

## Outcome

Workshop ACs, canvas, LOCAL curl, and the running Spring stub use the **same** JSON field names for create/list/get. Agents following [[ops/tickets/E2E-001]] can POST a minimal place without guessing.

## Notes

Found on the E2E workshop 2026-09-06 happy path ([[ops/runs/e2e-place-stub-mcp-chaos-2026-09-06]]; also the happy-path-only pass on superseded PR #28). Chaos was not required to see this.

| Surface | Create fields |
|---|---|
| [[ops/tickets/E2E-001]] + canvas | `name`, `categorySlug` required; optional `town`, `countySlug`, `lon`+`lat` |
| Running stub (PR #21) | `name`, `categoryId`, `countyId` required (`@NotBlank`); `latitude`/`longitude` |
| POST with E2E-001 names | **400** (`countyId must not be blank`) |

Happy-path create **does** work with the stub contract (`categoryId`/`countyId`). **Workshop Lead:** stub wins. Docs only (no Java): E2E-001 ACs + canvas use `categoryId` / `countyId` / `latitude` / `longitude`. [[ops/workflow/LOCAL]] curl already matches.

## Verify

- [x] One documented create body works in E2E-001 verify, [[ops/workflow/LOCAL]] curl, and `POST /api/v1/places` on default compose
- [x] `GET /api/v1/places` and `GET /api/v1/places/{id}` examples use the same id/slug rules
- [x] Canvas node text matches the chosen names
- [x] No chaos profile; no CI chaos

## Links

- Parent: [[ops/tickets/E2E-001]]
- Stub: [[ops/tickets/PRD-001]] (PR #21)
- Siblings: [[ops/tickets/WF-016]] · [[ops/tickets/WF-017]]
- Run: [[ops/runs/e2e-place-stub-mcp-chaos-2026-09-06]] · [[ops/runs/WF-018-close]]
- PR: https://github.com/tezball/my-island/pull/37
- Canon: [[ops/workflow/STACK-E2E-place-stub]] · [[ops/workflow/LOCAL]] · [[ops/workshops/e2e-place-stub]]
