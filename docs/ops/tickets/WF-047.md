---
id: WF-047
aliases: [WF-047]
title: Human and agent DX handbook
status: review
priority: P1
type: workflow
owner: automation-expert
area: ops
parent: "[[ops/tickets/WF-000]]"
plan: "[[ops/plans/WF-047]]"
pr: https://github.com/tezball/my-island/pull/99
blocked_reason:
cssclasses:
  - ticket
---

# WF-047 — Human and agent DX handbook

## Outcome

A human engineer and an AI agent can **clone, run, observe, and test** my-island from git without guessing. Durable copy lives at [[ops/workflow/DX]] plus [[ops/runbooks/CONFIRM_DEPLOY]]. Linked from [[ops/HOME]]. No new Cursor skill (point at existing ones). No product feature work.

## Notes

Docs-only. Worktree from `origin/main`. Ready PR; chat does not merge.

Handbook covers: `./scripts/app` vs `./scripts/dev`; ticket → GHA → automerge → Jenkins mock-prod (no agent SSH) → HTTP smoke; fishing-journals.com is **not** a GitHub `production` Environment; MCP pack + Cloud Agent PromQL fallback; password Guest + Google; `X-Catalog-Import-Key` for Place POST; VisitIntent private lists / public `beenCount`; test lanes; how to confirm deploy and debug.

Fix broken **doc examples** that still POST Places with no import header after [[ops/tickets/PRD-015]]. Do not invent secrets. Local compose defaults already in `compose.yml` may be named; mock-prod values stay in Jenkins env.

Do **not** add a SKILL.md. Catalog: [[ops/runbooks/ADD_SKILL]]. Point at clone-run, ops-loop, automation, mcp-observe.

## Verify

- [x] [[ops/workflow/DX]] is short, imperative, path-specific; audience human + agent
- [x] [[ops/runbooks/CONFIRM_DEPLOY]] has copy-paste health/info/places + PromQL fallback
- [x] [[ops/HOME]] (and company [[HOME]]) link the handbook
- [x] LOCAL / STACK-E2E / sim examples send `X-Catalog-Import-Key` (or GET-only)
- [x] `./scripts/app test` Place POST uses the import header (documented clone path)
- [x] No new SKILL.md; existing skills listed
- [x] No tokens, SSH keys, or mock-prod passwords in `docs/`
- [x] `python3 -m pytest ops/tests -q -m "not stack"`

## Links

- Plan: [[ops/plans/WF-047]]
- Parent: [[ops/tickets/WF-000]]
- Toolbox: [[ops/workflow/AGENT_DX]]
- Local: [[ops/workflow/LOCAL]]
- MCP: [[ops/workflow/MCP]]
- Pipeline: [[ops/workflow/PIPELINE]]
- Deploy: [[ops/runbooks/MOCK_PROD_DEPLOY]]
