---
id: WF-045
aliases: [WF-045]
title: Gatling trickle and weekly perf fail Jenkins plus Grafana
status: review
priority: P0
type: workflow
owner: eng-infra
area: ops
parent: "[[ops/tickets/WF-042]]"
plan: "[[ops/plans/WF-045]]"
pr: https://github.com/tezball/my-island/pull/98
blocked_reason:
cssclasses:
  - ticket
---

# WF-045 — Gatling trickle and weekly perf fail Jenkins plus Grafana

## Outcome

**Trickle smoke** and **weekly Gatling** failures mark **Jenkins red** and fire **Grafana / Alertmanager**. Agents read **both** via MCP (Jenkins job status + Grafana alerts, `--disable-write`). **Mute leftover fishing-journals email** — do not restore it ([[ops/tickets/INC-001]]).

## Notes

Alerts lock **C** (CEO 2026-09-19). Gatling shapes stay on [[ops/tickets/WF-042]]: light trickle on fishing-journals.com (ongoing feature smoke, not merge load); full perf weekly Jenkins cron and/or MCP/manual. This ticket is the **fail path**: red Jenkins + house Alertmanager, not leftover FJ `ApiDown` email.

House compose Alertmanager is `keep` today. Leftover VPS Alertmanager must stay `keep` (no SMTP). New rules/receivers are house Grafana/AM (agents MCP), not Terry’s inbox.

WF-009 (Alertmanager webhook → spawn agent) stays inbox — this ticket is **read** via existing MCP, not spawn. Observe datasources: [[ops/tickets/WF-041]].

## Verify

- [x] Trickle smoke failure → Jenkins job **red** (`gatling-trickle` cron `H/15`, `set -euo pipefail` + `./scripts/dev traffic`)
- [x] Weekly Gatling failure → Jenkins job **red** (`gatling-weekly` cron `H 6 * * 0`, `set -euo pipefail`)
- [x] Same failures fire Grafana / Alertmanager (house `notify_house_alertmanager.py` + Prometheus `CatalogHttpErrors`; not leftover FJ email)
- [x] Agents can read Jenkins status **and** firing alerts via MCP (Jenkins job color + Grafana/Prometheus rules; `--disable-write`)
- [x] Leftover FJ email stays muted ([[ops/tickets/INC-001]]); no SMTP in `docs/`
- [x] `python3 -m pytest ops/tests -q -m "not stack"`

## Links

- Plan: [[ops/plans/WF-045]]
- Gatling shapes: [[ops/tickets/WF-042]]
- Observe: [[ops/tickets/WF-041]]
- Mute leftover email: [[ops/tickets/INC-001]]
- Spawn later: [[ops/tickets/WF-009]]
- MCP: [[ops/workflow/MCP]]
- Policy: [[ops/company/DECISIONS]]
