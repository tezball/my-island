---
id: WF-043
aliases: [WF-043]
title: Chaos Monkey in CI for retries and fallbacks
status: review
priority: P0
type: workflow
owner: automation-expert
area: ci
parent: "[[ops/tickets/WF-000]]"
plan: "[[ops/plans/WF-043]]"
pr:
blocked_reason:
cssclasses:
  - ticket
---

# WF-043 — Chaos Monkey in CI for retries and fallbacks

## Outcome

**Chaos Monkey** runs **in merge CI** (Jenkins + GHA) using the house chaos overlay and/or Testcontainers, and proves **retries and default fallbacks**. **CI is the required chaos lane.** Do **not** move this job to cron. Chaos stays **off** the UI-less `unit` / `catalog` jobs (fast merge). **Do not** run Chaos Monkey against public https://fishing-journals.com/ on every deploy. MCP may still trigger a drill later.

## Notes

Chaos lock **C** + test-lanes lock (CEO 2026-09-19). Playwright is cron + MCP ([[ops/tickets/WF-011]]). Catalog API, Chaos Monkey, and ZAP are **on merge**. Gatling is trickle + weekly, not merge load ([[ops/tickets/WF-042]]). Overlay already exists: `compose.chaos.yml` + Spring profile `chaos` / `chaos-monkey`. Workshop drill [[ops/tickets/WF-017]] stays done. Default `./scripts/dev up` stays clean.

Dedicated Jenkins + GHA job — **not** folded into `unit` / `catalog`. Kill-application stays off. Do not assault the public test server as a deploy hook ([[ops/tickets/WF-040]]). Automerge waits on this job once wired (`needs`), but `unit`/`catalog` bodies stay chaos-off.

Same stream 1 as [[ops/tickets/WF-040]]. ZAP: [[ops/tickets/WF-044]]. Gatling trickle/weekly: [[ops/tickets/WF-042]]. Alerts: [[ops/tickets/WF-045]].

## Verify

- [x] Dedicated `chaos` job on Jenkins + GHA (house overlay and/or Testcontainers) on **merge**, not cron
- [x] Automerge `needs` includes `chaos` once the job exists
- [x] Job asserts retries / default fallbacks (not a second Gherkin contract)
- [x] `unit` and `catalog` do **not** enable Spring profile `chaos` or `compose.chaos.yml` (Playwright also stays off those jobs)
- [x] Deploy to fishing-journals.com does **not** start Chaos Monkey on every deploy
- [x] MCP workshop drill remains optional ([[ops/runbooks/STACK_E2E_PLACE_STUB]])
- [x] Kill-application stays off; no disk wipe; no git checkout drop
- [x] `python3 -m pytest ops/tests -q -m "not stack"`

## Links

- Plan: [[ops/plans/WF-043]]
- Parent: [[ops/tickets/WF-000]]
- Workshop drill (done): [[ops/tickets/WF-017]]
- Overlay: `compose.chaos.yml`
- Deploy (no public chaos): [[ops/tickets/WF-040]]
- ZAP: [[ops/tickets/WF-044]]
- Gatling: [[ops/tickets/WF-042]]
- Alerts (Jenkins red + Grafana): [[ops/tickets/WF-045]]
- Playwright (cron only): [[ops/tickets/WF-011]]
- CI: [[ops/workflow/CI]]
- Test mix: [[ops/workflow/TEST_STACK]]
- Policy: [[ops/company/DECISIONS]]
