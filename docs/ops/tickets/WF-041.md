---
id: WF-041
aliases: [WF-041]
title: House Prometheus and Loki from mock-prod
status: implement
priority: P0
type: workflow
owner: eng-infra
area: ops
parent: "[[ops/tickets/WF-032]]"
plan: "[[ops/plans/WF-041]]"
pr:
blocked_reason:
cssclasses:
  - ticket
---

# WF-041 — House Prometheus and Loki from mock-prod

## Outcome

House Prometheus and Loki data comes from the **test server** (https://fishing-journals.com catalog: Micrometer + structured logs). **Agents** read and act through **Grafana MCP over HTTP/SSE** (`mcp-grafana --disable-write`) against those datasources — **not** local-compose-only metrics. **Laptop Grafana** uses the **same** Prom/Loki (repo script / tunnel). Prometheus is **not** on the public internet. Agents never SSH.

## Notes

Observability lock **C** (CEO 2026-09-19). Today house Grafana/Prom/Loki run in local compose and scrape *this* VM’s catalog. Mock-prod overlay profiles those sidecars off. Public `grafana.fishing-journals.com` is leftover fishing-journals, **not** house Grafana. This ticket changes the **source of truth** to the test box and makes MCP + laptop Grafana point at it.

[[ops/tickets/WF-016]] (local compose MCP) stays done as the laptop workshop path. After this ticket, Cloud Agents **must not** be left on loopback PromQL as the only observe path. [[ops/tickets/WF-004]] stays **blocked** (generic staging/prod on [[ops/tickets/WF-010]]).

Same stream 1 as [[ops/tickets/WF-040]] (parallel with VisitIntent). Chaos CI is [[ops/tickets/WF-043]]. ZAP CI is [[ops/tickets/WF-044]]. Secrets in Jenkins / Cursor MCP settings, never `docs/`.

**2026-09-26:** Stay `implement` in the file, but do not start this ahead of [[ops/tickets/WF-040]]. When WF-040 leaves `implement`, set [[ops/tickets/PRD-032]] to `implement` before picking this ticket. Program: [`product/FREE-DIRECTORY.md`](../../product/FREE-DIRECTORY.md).

## Verify

- [ ] House Prometheus/Loki hold **fishing-journals.com** catalog metrics and logs (not only compose `catalog:8080`)
- [ ] Cloud Agent Grafana MCP is HTTP/SSE, `--disable-write`, querying **that** data (not local-compose-only)
- [ ] Laptop Grafana datasources are the **same** Prom/Loki (script/tunnel in `scripts/` or `ops/scripts/`)
- [ ] Prometheus scrape/remote_write is not published on the public internet
- [ ] Leftover `grafana.fishing-journals.com` is not treated as house
- [ ] WF-004 / WF-010 remain blocked
- [ ] No tokens in `docs/`
- [ ] `python3 -m pytest ops/tests -q -m "not stack"`

## Links

- Plan: [[ops/plans/WF-041]]
- Parent: [[ops/tickets/WF-032]]
- Deploy: [[ops/tickets/WF-040]]
- MCP pack: [[ops/tickets/WF-042]]
- Policy: [[ops/workflow/MCP]]
- Related (do not close): [[ops/tickets/WF-004]], [[ops/tickets/WF-016]]
- Canon: [`product/STACK.md`](../../product/STACK.md)
