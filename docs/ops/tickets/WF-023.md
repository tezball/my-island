---
id: WF-023
aliases: [WF-023]
title: Re-home living markdown under docs/ (Obsidian vault)
status: review
priority: P0
type: workflow
owner: automation-expert
area: ops
parent: "[[ops/tickets/WF-000]]"
plan: "[[ops/plans/WF-023]]"
pr: https://github.com/tezball/my-island/pull/52
blocked_reason:
---

# WF-023 — Re-home living markdown under docs/ (Obsidian vault)

## Outcome

All living application/company **markdown** lives under `docs/` except root `README.md`. Obsidian opens **`docs/`** only. Product canon is intact at `docs/product/`. Company OS notes are at `docs/ops/`. Ticket frontmatter is unchanged in meaning. E2E workshop (STACK-E2E, E2E-001, canvas, runbooks) is under `docs/ops/…`. CI Python/SQL/compose stay at `ops/scripts`, `ops/tests`, `ops/observability`.

## Notes

Terry LOCK (confirmed via Ops/PA). Supersedes the WF-020 “do not re-home markdown to `docs/ops`” hotfix: that ticket kept **CI runtime** at repo-root `ops/` (tests, requirements, Dockerfile `COPY`). This ticket moves **markdown only**.

Out of scope: WF-016 grants (#48), Spring feature code beyond path fixes.

## Verify

- [x] No living `.md` at repo root except `README.md` (no root `HOME.md`, no root `AGENTS.md`)
- [x] `docs/product/STACK.md` exists; `product/STACK.md` does not
- [x] `docs/ops/tickets/` has required frontmatter; `python3 ops/scripts/board_sync.py` writes `docs/ops/BOARD.md`
- [x] E2E canvas/brief/STACK-E2E/E2E-001 under `docs/ops/`
- [x] `python3 -m pytest ops/tests -q -m "not stack"` (66 passed)
- [x] `./services/catalog/mvnw -B test` (12 tests, BUILD SUCCESS)
- [x] `REQUIRE_STACK=1 python3 -m pytest ops/tests -q` (70 passed)

## Links

- Plan: [[ops/plans/WF-023]]
- PR: https://github.com/tezball/my-island/pull/52
- Run: [[ops/runs/WF-023-implement]]
