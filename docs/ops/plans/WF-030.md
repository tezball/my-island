---
id: WF-030
ticket: "[[ops/tickets/WF-030]]"
status: approved
cssclasses:
  - plan
---

# Plan: Vault OS UX — colour, dashboards, role homes

## Outcome

Vault looks and navigates like Jira + Confluence + eng docs: colour system, role dashboards (Bases + Dataview), templates, workshop with every persona. Done when verify on [[ops/tickets/WF-030]] is checked and a PR is open.

## Out of scope

Application code, Jenkins/Actions, consumer PWA theme, replacing `board_sync.py` as ticket SoT, prod/secrets.

## Steps

1. Visual system: `company-os.css` + enable snippet; [[ops/company/VAULT_DESIGN]].
2. Roster: architecture, business, eng-qa, eng-security + escalation update.
3. `ops/dashboards/*` markdown + `.base` per persona; Dataview fallbacks.
4. Templates (dashboard, workshop, role) + stamp cssclasses on hubs; NAMING.
5. Workshop brief + multi-role canvas; workshops `_index`.
6. Rebuild `HOME.md`; fix `ops/data` Dataview paths; PLUGINS / ops HOME / AGENTS.
7. board_sync; PR; status `review`.

## Verify

- [ ] Snippet enabled; dashboards use `cssclasses`
- [ ] Each role base filters on ticket frontmatter
- [ ] Dataview paths use `ops/tickets`
- [ ] Workshop + canvas linked from HOME

## MCP / env

Neither. Markdown + Obsidian config only.
