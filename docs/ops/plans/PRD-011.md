---
id: PRD-011
ticket: "[[ops/tickets/PRD-011]]"
status: approved
cssclasses:
  - plan
---

# Plan: Place detail API + PWA route

## Outcome

Visitor can open a **Place detail** from Explore: facts from catalog, map pin, directions deep-link, nearby list, share URL seam. Same Vite app as [[ops/tickets/PRD-003]] (`web/`). Catalog expands only fields MVP needs that are missing.

**Done when** `GET /api/v1/places/{idOrSlug}` powers `/places/:slug` (or equivalent) and ticket Verify on [[ops/tickets/PRD-011]] is checkable. Placeholder photos OK.

## Out of scope

- Check-off wiring ([[ops/tickets/PRD-012]]); My Places ([[ops/tickets/PRD-013]])
- Auth ([[ops/tickets/PRD-010]]); booking; GPX trails
- Image CDN / upload

## Steps

1. Confirm PlaceResponse fields vs MVP PLC-*; Flyway expand only if required.
2. PWA route + bottom-sheet → detail navigation from Explore.
3. Nearby: simple distance query or client-side from published list for seed size.
4. Seams: disabled check-off slot; share meta later on PRD-014 if needed.

## Verify

- [ ] Detail shows in-scope Place fields (no live rates)
- [ ] Directions open in device maps
- [ ] Nearby listed
- [ ] No Next.js; no booking columns

## MCP / env

`./scripts/app start` + Vite `:5173`.
