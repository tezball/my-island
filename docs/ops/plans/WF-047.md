---
id: WF-047
ticket: "[[ops/tickets/WF-047]]"
status: approved
cssclasses:
  - plan
---

# Plan: Human and agent DX handbook

## Outcome

One handbook plus one confirm-deploy runbook so a human or agent can run, observe, and test without assembling the loop from six notes. Linked from HOME. Broken Place-POST examples in clone/run docs (and the `./scripts/app test` smoke they teach) send the import header. No new skill. No product slice.

## Out of scope

- New `.cursor/skills/` (ADD_SKILL would require that; this ticket points at existing skills)
- Implementing Chaos/ZAP/Playwright/Gatling jobs (tickets already exist)
- Agent SSH, GitHub Environment `production`, secrets in notes
- Closing or implementing PRD-010 / WF-041–046 in this PR

## Steps

1. Land [[ops/workflow/DX]] covering clone/run, ticket loop, test box, MCP, auth/writes, test lanes, confirm, debug.
2. Land [[ops/runbooks/CONFIRM_DEPLOY]] with the HTTP + PromQL commands.
3. Link from [[ops/HOME]], company [[HOME]], [[AGENT_DX]], workflow/runbook indexes.
4. Fix LOCAL / sim / STACK-E2E curl examples and `scripts/app` smoke to send `X-Catalog-Import-Key`.
5. Vault pytest freeze. Ready PR. Do not merge from chat.

## Verify

- [ ] Handbook + runbook on `main` after CI squash-merge
- [ ] HOME links DX
- [ ] Place POST examples include the import header or are GET-only
- [ ] Vault `not stack` green
- [ ] No secrets in notes

Implement bar (company draft): [[ops/workflow/DOD]] — docs + vault freeze.

## MCP / env

Neither. Local compose optional. Do not put `CATALOG_IMPORT_KEY` mock-prod values in git.
