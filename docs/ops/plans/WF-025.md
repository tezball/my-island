---
id: WF-025
ticket: "[[ops/tickets/WF-025]]"
status: approved
---

# Plan: No prod; auto-review approve and merge ready PRs

## Outcome

CEO lock is in the vault and in an always-applied Cursor rule: **no production, probably never.** Ready PRs merge themselves after green CI. Done when SAFETY/LOOP/CI agree, the automerge job exists, and vault tests pass.

## Out of scope

- Merging **draft** PRs (plan PRs stay draft until marked ready)
- Fork PRs
- Cursor Automations UI enablement ([[ops/tickets/WF-003]])
- Closing staging tickets ([[ops/tickets/WF-010]], [[ops/tickets/WF-004]])
- Prod GitHub Environment
- Merging this PR from chat if CI can do it

## Steps

1. File [[ops/tickets/WF-025]] (skip WF-024 — claimed by the Apple Silicon PostGIS PR). Owner: automation-expert.
2. Sign the lock in [[ops/company/DECISIONS]] and `.cursor/rules/no-prod.mdc`.
3. Relax [[ops/workflow/SAFETY]], [[ops/workflow/LOOP]], [[ops/workflow/CI]], charter/home/agents/skills/hooks: chat agents still do not merge; Actions squash-merges ready same-repo PRs. No prod fleet to deploy to.
4. Add CI job `automerge` after `unit` + `catalog` + `stack`: approve (best-effort) + squash merge. Skip drafts and forks.
5. Vault test pins the job guards and the decision text.

## Verify

- [ ] Decision + rule exist
- [ ] automerge job guards
- [ ] `python3 -m pytest ops/tests -q -m "not stack"`

## MCP / env

No compose required. No prod Grafana.
