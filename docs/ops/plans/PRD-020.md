---
id: PRD-020
ticket: "[[ops/tickets/PRD-020]]"
status: approved
cssclasses:
  - plan
---

# Plan: Host identity and listing onboard

## Outcome

Host role on the existing account; Host creates **draft** Stays via `/api/v1/host/…`. Drafts hidden from public search. Seed Host owns mock seed Stays. Public Place POST stays closed.

## Out of scope

- Self-publish (Admin [[ops/tickets/PRD-026]])
- Calendar UI ([[ops/tickets/PRD-021]])
- Claim-an-existing-POI flow (skipped)

## Steps

1. Role `HOST` (and keep `GUEST`, `ADMIN`). Seed Host in compose env, not docs.
2. Host APIs: create/update draft Stay + Units + photos metadata. Server-side authz.
3. PWA `/host/onboard` wizard (phone-first). Default `instant_book=false` (request-to-book).
4. Testcontainers: Guest 403; Host draft omitted from public GET list.

## Verify

- [ ] WF-046 still holds for unauthenticated Place writes
- [ ] Seed Host login
- [ ] Draft not public
- [ ] No second API / Next BFF

Implement bar: [[ops/workflow/DOD]].

## MCP / env

Local compose. Depends on PRD-016 schema + PRD-010 session.
