---
id: PRD-012
ticket: "[[ops/tickets/PRD-012]]"
status: approved
cssclasses:
  - plan
---

# Plan: Check-off and visits API

## Outcome

One-tap ✓ from list, map sheet, and detail. Works **before account** (local store). After [[ops/tickets/PRD-010]] login, `POST /api/v1/me/visits/migrate` upserts without dupes. Visit table already from PRD-001.

## Out of scope

- Visit photos; bulk tick; log-a-place-not-in-directory
- My Places UI ([[ops/tickets/PRD-013]])

## Steps

1. Visit write/read HTTP under session; anonymous local ticks in PWA (IndexedDB/localStorage).
2. Implement migrate per PRD-010 contract (idempotent `clientTickId` / user+place).
3. Wire enabled check-off on Explore/detail; optimistic UI + offline queue.

## Verify

- [ ] One tap; no required modal
- [ ] Pre-account local + migrate without duplication
- [ ] `visitType` + `datePrecision` stored
- [ ] Offline queue; untick works
- [ ] No booking

## MCP / env

Local compose + PWA. Depends on PRD-010 password auth (no OIDC stub).
