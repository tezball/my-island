---
id: WF-031
ticket: "[[ops/tickets/WF-031]]"
status: approved
cssclasses:
  - plan
---

# Plan: Jenkins house CI — local compose + JCasC

## Outcome

`./scripts/dev up` starts Jenkins with JCasC from git, durable `ops_jenkins` volume, `local-ci` + GitHub multibranch + deploy stub. Done when verify on [[ops/tickets/WF-031]] passes and a PR is open.

## Out of scope

- Legacy `docs/automation/` Jenkins restore
- GitHub Environment `production`
- Live mock-prod deploy ([[ops/tickets/WF-032]] / [[ops/tickets/WF-010]])
- Removing GHA required checks in this PR (dual-run)

## Steps

1. Policy: DECISIONS unlock greenfield Jenkins; SAFETY/CI/STACK wording.
2. `ops/jenkins` Dockerfile + plugins + Casc + Job DSL seed jobs.
3. Root `Jenkinsfile`; compose service port 8085; `.env.example`.
4. `scripts/dev` starts Jenkins locally; `SKIP_JENKINS=1` in GHA.
5. Workshop, canvas, runbook; vault test pin.
6. Boot stack; confirm login + jobs; document token handoff.

## Verify

- [ ] Jenkins healthy on `./scripts/dev up`
- [ ] Jobs `local-ci`, `my-island`, `deploy-mock-prod` present
- [ ] Casc + plugins in git; volume named `ops_jenkins`
- [ ] GHA stack still uses `SKIP_JENKINS=1`

## MCP / env

Local compose. Optional `JENKINS_GITHUB_TOKEN` in `.env`.
