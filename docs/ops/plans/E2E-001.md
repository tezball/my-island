---
id: E2E-001
ticket: "[[ops/tickets/E2E-001]]"
status: approved
cssclasses:
  - plan
---
# Plan: Place listing stub — workshop close-out

## Outcome

Close [[ops/tickets/E2E-001]] against the **already-landed** catalog stub. PRD-001 (#21) shipped `POST` / `GET` create → list → get. This plan maps every ticket Verify checkbox to an existing test or sim step. **Do not rebuild the API. Do not add a consumer UI.**

**Done when** this plan is approved on `main` and the mapped checks are still green. Recommended close: set ticket `status: done` (skip `implement`). Implement only if remaining gaps below are worth a tiny follow-up PR.

JSON contract stays **stub wins** ([[ops/tickets/WF-018]]): `categoryId` / `countyId` / `latitude` / `longitude`. Do not send `categorySlug` / `countySlug` / `lat` / `lon`.

## Out of scope

- New catalog endpoints, fields, or a second Place JSON shape
- Consumer PWA / Explore UI ([[ops/tickets/PRD-003]])
- Auth on this stub (workshop exception stays documented)
- Booking / Stripe / inventory / visits HTTP / partner portal / images / search or pagination polish / bulk import
- Chaos on required CI (`unit` / `catalog` / `stack`)
- Merge, prod deploy, secrets in notes

## Verify map (ticket checkbox → proof)

| Ticket Verify | Existing proof | Gap? |
|---|---|---|
| `POST /api/v1/places` creates: `name` required, `categoryId` required (`poi` \| `experience` \| `campsite` \| `bnb`), optional `town`, optional `countyId`, optional `latitude`+`longitude` → PostGIS when **both** present | Happy create: `CatalogTest.createListGetPlace` (201, `poi`, `kerry`, town, both coords). Seeds: `categoriesAndFacilitiesAreRows`, `countiesAreDataIncludingNi`, `ops/tests/test_catalog.py` Flyway seeds. Town optional: `provenancePersistsOnCreate` (null town). Both coords → PostGIS: `locationIsWrittenAsPostgisPoint` + `V5__place.sql` trigger. Sim: `ops/scripts/sim_place_listing.py` `payload_for` + `create_place` (201). Field names: `test_payload_uses_stub_field_names` | **Accepted stub vs AC wording:** ticket says optional `countyId`; live stub is `@NotBlank` + DB `NOT NULL`. WF-018: stub wins. Do not loosen. |
| Response returns `id` (UUID) + resource shape | `createListGetPlace`: `PlaceResponse.id`, category, county, facilities, `Location` header. `PlaceResponse.id` is `UUID`. Sim asserts JSON `id` | No |
| `GET /api/v1/places` lists places (includes the created one) | Sim `list_contains`: unfiltered list includes `id`. `createListGetPlace`: `GET ?countyId=kerry` contains slug. Pytest: `test_happy_path_three_iterations`, `test_live_compose_create_list_get` (`@pytest.mark.stack`) | No |
| `GET /api/v1/places/{id}` returns one or 404 | `createListGetPlace` GET by id 200; `unknownPlaceIs404AndUnknownCategoryIs400` GET unknown 404. Sim `get_place` | No |
| Missing `name` / unknown `categoryId` → **400** not 500 | Unknown category: `unknownPlaceIs404AndUnknownCategoryIs400`. Missing name: `@NotBlank` on `CreatePlaceRequest.name` + `ApiExceptionHandler` `MethodArgumentNotValidException` → 400 | **Coverage only:** no `CatalogTest` POST with blank/missing `name`. Behavior exists. Not a rebuild. |
| No auth for workshop stub (document as workshop exception) | Ticket Notes; [[ops/workshops/e2e-place-stub]]; [[ops/runbooks/PLACE_LISTING_SIM]]; [[ops/runbooks/STACK_E2E_PLACE_STUB]]; [[ops/workflow/STACK-E2E-place-stub]]. Vault: `test_e2e_001_place_stub_workshop` asserts `workshop exception`. Tests and sim send no `Authorization` | Keep documented. Do not add Spring Security here. |
| No booking / Stripe / inventory columns | `CatalogTest.placeHasPartnerIdSeamAndNoBookingColumns`; `ops/tests/test_catalog.py` `test_catalog_has_no_stripe_or_booking_surface` | No |
| Automated tests: create → list contains `id` → get by id; CI green | `createListGetPlace` + sim loop. CI: `.github/workflows/ci.yml` `catalog` (`./mvnw -B test`), `stack` (live sim via pytest `stack`), `unit` (`not stack`, no chaos). Chaos freeze: `test_e2e_place_stub_mcp_chaos_followups` | No |
| Out: UI, images, visits API, partner portal, search polish, pagination polish, bulk import | `PlaceController` is POST + GET list/get only. Visit table is PRD-001 schema, no visit HTTP. No consumer `web/` app. Bulk import is [[ops/tickets/PRD-008]] (done, drafts) / [[ops/tickets/PRD-002]] (seed) | No. Do not add a UI on this ticket. |

## Remaining gaps (only these)

1. **Missing-name 400 is untested** in `CatalogTest`. Jakarta `@NotBlank` already returns 400. Optional one test if a human opens `implement`; not required for the workshop bar (plumbing + demo-able tests).
2. **Lat XOR lon → 400 is not implemented.** Ticket AC says one coord without the other → 400. Live stub: `V5__place.sql` `place_sync_location` sets `location` NULL when either is null and still inserts (201). Happy path always sends **both**. Workshop bar does not need XOR 400. **Accept as stub behavior** unless a human asks for a tiny validation + test.

No other HTTP gaps for create/list/get. WF-016 / WF-017 / WF-018 are **done** (MCP attach, STACK-E2E drill, field names). Chaos overlay stays workshop-only.

## Recommendation

**Set ticket `done` after this plan is approved and tests still green.** Skip `implement`. Do not rebuild create/list/get.

Move to `implement` **only** to land remaining gaps: (a) `CatalogTest` missing-name 400, and/or (b) XOR lat/lon 400. That is a few lines, not a new API.

## Steps (this planner PR)

1. Write this plan. Link it from [[ops/tickets/E2E-001]] (`status: plan`, `plan: "[[ops/plans/E2E-001]]"`).
2. Move vault freeze asserts from `ready` → `plan` (`test_e2e_001_place_stub_workshop`, `test_e2e_place_stub_mcp_chaos_followups`, `test_product_milestones_freeze`). Keep type `story`, P0, owner `eng-backend`. Do **not** set `done` (workshop-pass run still says the 2026-09-06 pass left status `ready`).
3. `python3 ops/scripts/board_sync.py`. `python3 -m pytest ops/tests -q -m "not stack"`.
4. Run note [[ops/runs/E2E-001-plan]]. Stop. No catalog Java. No chaos on CI.

## Verify (this plan PR)

- [ ] Ticket `status: plan` and `plan: "[[ops/plans/E2E-001]]"`
- [ ] Every ticket Verify row in the map above cites a real test or sim step
- [ ] Remaining gaps listed; recommendation is done-after-approve (or gaps-only implement)
- [ ] Workshop no-auth exception still documented on the ticket
- [ ] JSON names still `categoryId` / `countyId` / `latitude` / `longitude`
- [ ] `python3 -m pytest ops/tests -q -m "not stack"`
- [ ] No application code; no consumer UI; no chaos on required CI

## MCP / env

Neither for this planner PR. Implementer (only if gaps): `./scripts/dev up`, `services/catalog/mvnw test`, `./scripts/sim-place-listing.sh`. Cloud Agents: PromQL HTTP fallback if grafana MCP is missing ([[ops/workflow/MCP]]).

## Provenance

Harvested from draft Cloud Agent branches (2026-09-06) into `main` company state. Edited for current LOOP (main-first, prefer no human gate, Jenkins house CI).
