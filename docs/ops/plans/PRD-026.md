---
id: PRD-026
ticket: "[[ops/tickets/PRD-026]]"
status: approved
cssclasses:
  - plan
---

# Plan: Admin moderation, users, catalog

## Outcome

Admin role routes: users, publish/reject draft Stays, review flags, audit, seed/import trigger (CI job or authenticated admin import — **not** public Place POST). Extend PRD-014 admin if it exists; do not add a second app.

## Out of scope

- Disputes ([[ops/tickets/PRD-027]])
- Support tickets ([[ops/tickets/PRD-029]])
- Waiving [[ops/tickets/PRD-009]] for real lead publish

## Steps

1. `/api/v1/admin/users`, `/admin/stays/{id}/publish|reject`, `/admin/reviews/queue`.
2. PWA `/admin` role-gated.
3. Audit table for those actions.
4. Testcontainers: non-admin 403; publish appears in Guest search.

## Verify

- [ ] WF-046 still closed to anonymous
- [ ] Deactivate blocks book + host write
- [ ] Totals endpoint
- [ ] No Next.js BFF

Implement bar: [[ops/workflow/DOD]].

## MCP / env

Local compose. Seed Admin in env, not docs.
