---
id: WF-020
ticket: "[[ops/tickets/WF-020]]"
status: approved
---

# Plan: Restore live ops/ after mistaken docs move

## Outcome

Repo-root `ops/`, `data/leads/`, and `product/` are the live trees again. CI unit / catalog / stack jobs can find `ops/tests/requirements.txt` and `product/STACK.md`. Archaeology stays under `docs/`.

## Out of scope

- Re-homing CI, Dockerfile, or compose to `docs/ops`
- Changing catalog API, STACK.md content, or consumer UI
- Merging this PR (CEO merges when green)
- Stealing WF-016 / WF-017 / WF-018 / WF-019

## Steps

1. Branch from latest `main` (`be8562e`).
2. `git mv docs/ops ops`, `git mv docs/data/leads data/leads`, `git mv docs/product product`.
3. Keep `docs/README.md`, `docs/leads/CAMPSITE_LEADS.md`, `docs/automation/`.
4. Keep root `compose.yml` as STACK-E2E source of truth (`ops/compose.yml` is the include wrapper).
5. File this ticket as WF-020 (next free after 018; 019 is PR #31).
6. Open PR. Do not merge.

## Verify

- [x] `ops/tests/requirements.txt` at repo root
- [x] `data/leads/schema.json` at repo root
- [x] unit + catalog + compose stack green (local); GitHub Actions on PR #33
