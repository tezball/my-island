---
id: PRD-014
ticket: "[[ops/tickets/PRD-014]]"
status: approved
cssclasses:
  - plan
---

# Plan: Launch quality (local MVP slice)

## Outcome

Local launch bar: PWA install + offline shell, GDPR export/erasure seams complete with visits, PromQL/`up{job="catalog"}` + a core-path alert, backup restore **runbook**, rate limits on auth, structured logs. Success **threshold numbers** stay a human product call — leave a blank table for Terry.

## Out of scope

- Host pick / staging ([[ops/tickets/WF-010]]); remote Grafana MCP ([[ops/tickets/WF-004]])
- Counsel sign-off ([[ops/tickets/PRD-009]])
- Inventing kill metrics without product

## Steps

1. Complete export/delete with visits; consent copy if missing.
2. Alert + dashboard panels queryable locally.
3. Document backup restore drill (`docker compose` volumes).
4. Playwright required when UI exists ([[ops/tickets/WF-011]]) — green job only.
5. Blank thresholds table in ticket for human fill.

## Verify

- [ ] §7 signals visible locally via MCP or PromQL
- [ ] GDPR export + delete
- [ ] PWA install + offline shell
- [ ] Backup restore documented
- [ ] Thresholds table present (values may be empty until human)

## MCP / env

Local Grafana `:3030`, Prometheus `:9091`.
