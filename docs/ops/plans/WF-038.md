---
id: WF-038
ticket: "[[ops/tickets/WF-038]]"
status: approved
cssclasses:
  - plan
---

# Plan: Git worktrees for multi-session Cursor

## Outcome

The company OS tells every agent how to run **one ticket per Cursor session** without checking `main` out from under another session. Primary clone = `main`. Feature work = sibling worktree + branch + PR. Mock-prod VPS tracks `main` only. Done when verify on [[ops/tickets/WF-038]] is checked.

## Out of scope

Per-worktree Compose port offsets. Nested `.cursor/worktrees` inside the primary clone. A `scripts/worktree` wrapper (file later if the commands hurt). Moving WF-032 / other in-flight branches in this PR.

## Steps

1. Policy note [[ops/workflow/WORKTREES]]: three layers (main / worktree PR / mock-prod VPS), layout, compose singleton, Obsidian, Cloud Agents, reviewer-without-tree.
2. Runbook [[ops/runbooks/WORKTREE]]: add / open / test / remove commands.
3. Point [[ops/workflow/LOOP]], [[ops/runbooks/TICKET_LOOP]], ops-loop skill/rule, LOCAL, AGENT_DX, PIPELINE at the new default.
4. Vault test lists the new notes. `board_sync.py`. No `services/` / `web/` code.

## Verify

- [ ] Handbook answers: where is `main`, where is the PR branch, what the VPS runs, how two sessions share Docker
- [ ] Implementer skill no longer says “no worktree by default”
- [ ] No `compose.prod` / GitHub Environment `production`

## MCP / env

Neither. Markdown + skill/rule only. Local compose stays a laptop singleton ([[ops/workflow/LOCAL]]).
