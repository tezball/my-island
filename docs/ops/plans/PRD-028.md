---
id: PRD-028
ticket: "[[ops/tickets/PRD-028]]"
status: approved
cssclasses:
  - plan
---

# Plan: Help center and policies

## Outcome

Public `/help` in the PWA: booking, hosting, cancellation policies, mock payments, privacy pointer, cookies. Checkout links these. English. Brand OPEN.

## Out of scope

- Next.js marketing site
- Counsel-signed legal for a real consumer launch (`gate: human` not required for mock-prod stubs)
- Support inbox ([[ops/tickets/PRD-029]])

## Steps

1. Markdown (or JSON) content in `web/` or `docs/product/policies/` rendered by the PWA.
2. Routes `/help`, `/help/cancellation`, `/help/privacy`, `/help/hosting`, `/help/payments`.
3. PRD-018 replaces stub hrefs when both exist — this ticket may land first.

## Verify

- [ ] Logged-out readable
- [ ] No Fáilte / StayÉire lock
- [ ] No secrets
- [ ] No Next.js BFF

Implement bar: [[ops/workflow/DOD]].

## MCP / env

No compose required if pages are static in the PWA; still run `web` unit tests.
