---
id: WF-016
ticket: "[[ops/tickets/WF-016]]"
status: approved
---

# Plan: Cloud Agent mcp-grafana and Postgres-RO against local compose (docs / MCP attach)

## Outcome

Cloud Agents know how to **attach** (or honestly fall back from) grafana + postgres MCP against local compose. Done when MCP.md / LOCAL / STACK-E2E / AGENTS.md document dashboard **stdio** vs HTTP PromQL, vault tests cover those docs, and PR #46 links this ticket. Catalog `ops_reader` SELECT grants are **Engineering** on a separate agent — not this PR.

## Out of scope

- Flyway / SQL grants for `ops_reader` on `catalog.place` (Engineering; TODO until their PR exists)
- Adding `postgres-catalog` MCP DSN (belongs with grants)
- Staging/prod remote Grafana MCP ([[ops/tickets/WF-004]])
- Always-on EU staging ([[ops/tickets/WF-010]])
- Product UI, chaos in CI, merge, secrets

## Steps

1. Document Cloud Agent attach in [[ops/workflow/MCP]], [[ops/workflow/LOCAL]], [[ops/workflow/STACK-E2E-place-stub]], `AGENTS.md`, and the STACK-E2E runbook: laptop `.cursor/mcp.json` does **not** follow Cloud Agents; `.cursor/environment.json` has no MCP attach field. Human adds matching **stdio** servers on cursor.com (MCP dropdown / team Integrations) so they run **in the VM** against `127.0.0.1:3030` / `5433`. HTTP MCP is proxied off-VM — do not point it at loopback.
2. Document HTTP PromQL fallback (already in the STACK-E2E runbook): Prometheus `:9091` and Grafana `/api/ds/query`. SSE / `mcp-remote` are not supported on Cloud Agent VMs.
3. Cross-link Engineering grants PR when it exists. Until then, leave a **TODO** pointer on the ticket and in MCP.md.
4. Vault tests for docs, ticket/plan/board. No grants SQL in this PR.

## Verify

- [x] MCP.md / LOCAL / STACK-E2E / AGENTS.md document Cloud Agent stdio-on-dashboard vs HTTP fallback
- [x] `.cursor/mcp.json` has `grafana --disable-write` and `postgres` → db `ops` (no catalog DSN here)
- [x] No grants SQL / Flyway / `ensure_catalog_reader_grants` in this PR
- [x] TODO pointer for Engineering catalog SELECT grants
- [x] `python3 -m pytest ops/tests -q -m "not stack"` green
- [x] No secrets; no merge; CI yaml has no chaos profile

## MCP / env

Local compose is enough to prove HTTP PromQL. This Cloud Agent toolbox has no `grafana` / `postgres` MCP until a human adds dashboard stdio. Prod Grafana is not required. Catalog `place` SELECT waits on Engineering.
