---
id: PRD-019
ticket: "[[ops/tickets/PRD-019]]"
status: approved
cssclasses:
  - plan
---

# Plan: Guest trips, cancel, refund

## Outcome

Guest **Trips** UI + cancel with refund preview. Policies: flexible (full until 24h before), moderate (full until 5 days; 50% after), strict (50% until 7 days; none after). Mock PSP refund. Check-in notes 48h before.

## Out of scope

- Admin dispute override ([[ops/tickets/PRD-027]])
- Host-initiated cancel (can be a follow-up in PRD-022 if needed)
- Live bank refunds

## Steps

1. `GET /api/v1/me/trips` upcoming/past/cancelled.
2. `POST /api/v1/me/bookings/{id}/cancel` returns refund quote then confirm flag.
3. Policy function pure + unit tested; nights return to `open`.
4. PWA `/trips`. Private; 403 cross-Guest.

## Verify

- [ ] Policy math table in Testcontainers
- [ ] Refund recorded on mock PSP + ledger seam for PRD-025
- [ ] Authz
- [ ] No Next.js BFF

Implement bar: [[ops/workflow/DOD]].

## MCP / env

Local compose. Depends on [[ops/tickets/PRD-018]].
