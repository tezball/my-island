---
id: PRD-035
ticket: "[[ops/tickets/PRD-035]]"
status: approved
cssclasses:
  - plan
---

# Plan: Phone-first look and feel refactor

## Outcome

The live PWA is visually refit for a phone: photo-led cards, readable type, one-thumb Explore / detail / journey / role homes. Stack stays Vite + React + TypeScript talking to Spring. Done when those screens match the directory, not a booking site.

## Out of scope

- Next.js, a BFF, or a new component framework
- Checkout, prices, date pickers
- PRD-012 one-tap and PRD-013 county totals / CSV
- A new brand lock (public name stays open — [`product/NAMING.md`](../../product/NAMING.md))
- Promoting [[ops/tickets/PRD-031]] on its own ahead of this ticket
- GitHub Environment `production`, `compose.prod`, prod SSH

## Steps

1. Read [`product/LOOK.md`](../../product/LOOK.md) as mood (photo-led, phone 390-wide, Ireland-specific without pastiche). Implement in the existing `web/` CSS and components. Do not add a design-tool export as the app.
2. Restyle the screens that exist when this ticket starts: Explore list + map, place detail, `/lists` journey, auth, and the role homes from [[ops/tickets/PRD-034]].
3. Kind chips must still tell campsite, B&B, experience, POI, and supplier apart.
4. If the place-detail back control ([[ops/tickets/PRD-031]]) is still broken, fix it in this PR. Leave that ticket’s status to the planner; do not start PRD-031 as its own implement session before this one.
5. After merge, mock-prod deploy from `main`. Confirm the phone layout on https://fishing-journals.com (info SHA matches `main`, then the restyled Explore). Agents never SSH. Playwright stays cron ([[ops/tickets/WF-011]]), not the merge gate.

## Verify

- [ ] Phone-first visual pass on the current PWA
- [ ] Vite + React + Spring unchanged
- [ ] No checkout UI
- [ ] Public host confirm
- [ ] Vitest if a helper is extracted

Implement bar (company draft): [[ops/workflow/DOD]].

## MCP / env

Local compose (`./scripts/app start`) for the PWA. Public HTTPS to confirm. No agent SSH.
