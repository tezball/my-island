---
id: WF-036
ticket: "[[ops/tickets/WF-036]]"
status: approved
cssclasses:
  - plan
---

# Plan: Second brain — atlas MOCs, folder colour, engineer notes

## Outcome

The vault is a git-shareable company second brain. Humans land on [[HOME]] then [[ATLAS]]. Colour in the file explorer and on note types. Dataview on every group index. Templates for wiki / note / ADR / meeting / MOC. Engineer notes live in `notes/`, not in tickets. Done when verify on [[ops/tickets/WF-036]] is checked and a PR is open.

## Out of scope

Moving `ops/tickets` or `product/` paths. Replacing `board_sync.py`. Product PWA theme. Plugin binaries. Bulk-retagging every existing ticket.

## Steps

1. Overlay IA: `ATLAS.md` + `atlas/<group>.md` Maps of Content. Do not relocate tickets.
2. Engineer lane: `notes/` (permanent), `notes/adr/`, `notes/meetings/`. Default new-file folder → `notes`.
3. Visual: expand `company-os.css` (folder colours, note-type washes, custom callouts). Graph colour groups. Bookmarks.
4. Templates + NAMING + PLUGINS + VAULT_DESIGN. Dataview on indexes.
5. Workshop + canvas. HOME / ops HOME / README point at the atlas.
6. Vault tests; board_sync; PR; status `review`.

## Verify

- [ ] Atlas groups exist and link both ways to the folders they describe
- [ ] Explorer CSS keys off `data-path` (no Iconize binary required)
- [ ] Tickets still `ops/tickets/<id>.md`; Dataview `FROM "ops/tickets"`
- [ ] No app code

## MCP / env

Neither. Markdown + Obsidian config only.
