---
id: PRD-023
ticket: "[[ops/tickets/PRD-023]]"
status: approved
cssclasses:
  - plan
---

# Plan: Guest-host messaging

## Outcome

`message_thread` per Booking (and per request-to-book). Guest and Host post messages. Mailpit locally. Not a social feed; not support tickets.

## Out of scope

- Support inbox ([[ops/tickets/PRD-029]])
- SMS/push
- Public profiles

## Steps

1. Flyway thread + message. Unique thread per booking_id.
2. APIs under `/api/v1/me/bookings/{id}/messages` and host twin. Participants only.
3. PWA thread on Trip + Host reservation panes.
4. Authz contracts; no Place-page leak.

## Verify

- [ ] Cross-user 403
- [ ] Chronological
- [ ] Mailpit on local new message (or explicit defer if Mailpit not wired — then log only, named in PR)
- [ ] House stack only

Implement bar: [[ops/workflow/DOD]].

## MCP / env

Local compose + Mailpit. Depends on [[ops/tickets/PRD-018]].
