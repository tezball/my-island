---
id: WF-023
ticket: "[[ops/tickets/WF-023]]"
status: approved
---

# Plan: Re-home living markdown under docs/

## Outcome

Obsidian vault is `docs/`. Living markdown is under `docs/` except root `README.md`. Product canon at `docs/product/`. Company OS notes at `docs/ops/`. CI still installs `ops/tests/requirements.txt`.

## Out of scope

- WF-016 catalog grants (#48)
- Spring feature code beyond path-string fixes
- Moving `.cursor/skills/**/SKILL.md` (Cursor requires that path)
- Merging the PR

## Steps

1. File [[ops/tickets/WF-023]]. Owner: automation-expert.
2. `git mv` `product/` → `docs/product/`; ops markdown folders → `docs/ops/`; `AGENTS.md` → `docs/AGENTS.md`; leads notes → `docs/data/leads/`; `.obsidian` → `docs/.obsidian`. Keep `ops/scripts`, `ops/tests`, `ops/observability`, `ops/compose.yml`.
3. Point `board_sync` / `next_ticket` / `new_ticket` `root()` at `docs/ops`. Board wikilinks: `[[ops/tickets/…]]`.
4. Rewrite vault-relative wikilinks and canvas file nodes. Update README, rules, skills, hooks, tests.
5. Verify unit + catalog + compose.

## Verify

- [ ] Root living markdown is only `README.md`
- [ ] Vault tests use `docs/` paths
- [ ] CI pytest path remains `ops/tests`
- [ ] E2E workshop files under `docs/ops/`
