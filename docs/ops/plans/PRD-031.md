---
id: PRD-031
ticket: "[[ops/tickets/PRD-031]]"
status: approved
cssclasses:
  - plan
---

# Plan: Mobile place-detail home/back control

## Outcome

On a phone-width Place page, the hero home/back control is a proper overlay: centered glyph, readable on the photo, 44px tap, `safe-area-inset-top`, still links to Explore. Done when Terry’s white-circle-over-photo control no longer looks broken. Small PWA CSS/markup bug only.

## Out of scope

- Implementing until [[ops/tickets/WF-050]] is on `main` (this PR is the e2e of review-gated automerge)
- [[ops/tickets/PRD-030]] map/list of been/want
- Directory MVP expansion, booking, Next.js, FastAPI, new routes besides Explore home
- Changing VisitIntent / auth

## Steps

1. **Wait.** Do not open the implement worktree until WF-050 has squash-merged to `main`. Then branch `prd/PRD-031-place-back` from that `main`.

2. **Fix the control, don’t remove it.** `PlacePage` hero `Link.back` (`to="/"`, “Back to Explore”) stays. In `.back` (and the Link if needed): center the chevron/`←` in the circle; keep ≥44px; add `top` with `env(safe-area-inset-top)` so it is not under the PWA status bar; contrast that works on a Commons hero (sheet circle is fine if the glyph is centered and not a naked underline). Phone-first.

3. **Prove at a phone width.** Vitest if you extract a tiny control. Otherwise CSS + a phone-viewport screenshot/recording in the implement PR. Playwright stays cron + MCP ([[ops/tickets/WF-011]]), not a merge gate.

4. **Automerge e2e.** After WF-050, this ready PR must **not** merge on green CI alone. A **different** actor (reviewer hat, not the implementer, not `github-actions[bot]`) submits Approve; then Actions squash-merges. Implementer does not Approve this PR. Chat does not `gh pr merge`.

## Verify

- [ ] Phone viewport: overlay home/back is aligned, tappable, safe-area aware, returns to Explore
- [ ] No PRD-030 / directory-MVP scope
- [ ] Implement started only after WF-050 on `main`
- [ ] Merge required a valid non-author Approve (WF-050 gate)

Implement bar (company draft): [[ops/workflow/DOD]].

## MCP / env

Local compose (`./scripts/app start`) for the PWA. Phone-width browser. No prod Grafana.
