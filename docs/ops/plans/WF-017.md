---
id: WF-017
ticket: "[[ops/tickets/WF-017]]"
status: approved
---

# Plan: STACK-E2E MCP vs compose chaos drill skill and runbook

## Outcome

Agents can run the place-stub stack E2E drill from a Cursor skill + vault runbook without inventing curl. Done when the skill fires on E2E-001 / STACK-E2E / chaos overlay / mcp-grafana catalog scrape, the runbook copies the proven happy-path and overlay commands, observe has an HTTP PromQL fallback, gaps are written, and required CI stays chaos-free.

Human instruction to implement: this Cloud Agent session (Engineering implements; Automation owns the ticket).

## Out of scope

- Attaching `mcp-grafana` / Postgres MCP to Cloud Agents ([[ops/tickets/WF-016]])
- Aligning E2E-001 JSON names with the stub ([[ops/tickets/WF-018]])
- Product UI, booking, or expanding [[ops/tickets/PRD-001]]
- Enabling chaos on default compose or required CI (`unit` / `catalog` / `stack`)
- Touching [[ops/tickets/WF-015]] (spine strip)
- Merge, prod, secrets

## Steps

1. Skill `.cursor/skills/stack-e2e-place-stub/SKILL.md` — short; description lists the fire phrases; body points at the runbook.
2. Runbook `ops/runbooks/STACK_E2E_PLACE_STUB.md` with copy-paste HTTP from [[ops/workflow/LOCAL]] / the 2026-09-06 run: `./scripts/dev up`, create/list/get + actuators, MCP-or-HTTP observe, overlay up, overlay down.
3. Rows on [[ops/runbooks/_index]] and [[ops/workflow/SKILLS]]. Pointer from [[ops/workflow/STACK-E2E-place-stub]].
4. Vault tests: skill description, runbook commands, CI yaml still has no chaos profile.
5. Ticket `status: implement` then `review` with PR URL. `python3 ops/scripts/board_sync.py`.

## Verify

- [ ] Skill description fires on E2E-001 / STACK-E2E / chaos overlay / mcp-grafana catalog scrape
- [ ] Runbook covers happy path, overlay command, HTTP surfaces, Grafana/PromQL HTTP fallback, restore default catalog
- [ ] Skill and runbook state required CI must not set Spring `chaos` or compose profile `chaos`
- [ ] [[ops/workflow/SKILLS]] row
- [ ] `python3 -m pytest ops/tests -q -m "not stack"` green
- [ ] No secrets; no merge; no product UI

## MCP / env

Local compose is enough to *prove* the documented curls. Cloud Agent toolbox today has **no** grafana/postgres MCP ([[ops/tickets/WF-016]]) — the runbook must fall back to HTTP. Prod Grafana is not required.
